Q: Suppose you set user.name='Prime' at the system level, user.name='Lane' at the global level, and user.name='TJ' at the local level. When running Git in that repository, which value will Git actually use?
A: TJ